<%
const layoutBody = `
<article class="page">
    <h1>${page.title}</h1>
    <div class="page-content">
        ${page.content}
    </div>
</article>
`;
%>

<%- include('layout', { body: layoutBody, title: page.title, config, meta, structuredData }) %>

